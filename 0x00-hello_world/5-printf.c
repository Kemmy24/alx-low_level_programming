#include <stdio.h>

/**
 * main - sends a formatted string to the standard output
 *
 * Return: when succesfull always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
